// Entry for multi-file schema: generator + datasource (models in sibling .prisma files).
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
